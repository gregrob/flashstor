#01reboot.conf

# Run at reboot - after 5min (when all containers are started in parallel they have time
# to start before the first backup)
#BACKUP_CRON_EXPRESSION="@reboot"
BACKUP_CRON_EXPRESSION="@reboot sleep 300 && "

# Notifications on both error and success
NOTIFICATION_LEVEL="info"
